@model IEnumerable<HRMManagement.Models.Display>
@{
    ViewData["Title"] = "Info";
    Layout = "~/Views/Shared/_DashboardLayout.cshtml";
}
<div class="card my-scrollbox-inner" id="resultContainer" style="margin: 10px;">
    <div id="table-header">
        <div class=" card-header d-lg-flex" style="justify-content:space-between">
            <h5>Nhân sự</h5>
            <div class="btn-group" role="group" aria-label="Button group with nested dropdown">
                <button type="button" class="btn btn-outline-info" asp-area="" asp-controller="" asp-action="">
                    <span class="material-symbols-outlined">
                        person_add
                    </span>
                </button>
                @* <div class="btn-group" role="group">
                    <button id="btnGroupDrop1"
                            type="button"
                            class="btn btn-outline-info dropdown-toggle"
                            data-bs-toggle="dropdown"
                            aria-haspopup="true"
                            aria-expanded="false">
                        Filters
                    </button>
                    <ul class="dropdown-menu" aria-labelledby="btnGroupDrop1">
                        <li class="dropdown-item">
                            <a class="test" tabindex="-1" href="#">Vị trí CV <span class="caret"></span></a>
                            <ul class="dropdown-menu">
                                <li class="dropdown-item">
                                    @foreach (var position in ViewBag.uniqueViTri)
                                    {
                                       <a class="" href="#">@position</a>
                                    }
                            </ul>
                        </li>
                    </ul>
                </div> *@
            </div>
        </div>
    </div>
    <div class="table-responsive text-nowrap">
        <table class="table table-hover">
            <thead>
                <tr>
                    <th>                     
                        <a class="nav-link dropdown-toggle"
                           href=""
                           role="button"
                           data-bs-toggle="dropdown"
                           aria-expanded="false">
                            Img
                        </a>
                    </th>
                    <th class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle"
                               href=""
                               role="button"
                               data-bs-toggle="dropdown"
                               aria-expanded="false">
                               Nhân sự
                            </a>
                    </th>
                    <th>
                        <a class="nav-link dropdown-toggle"
                           href=""
                           role="button"
                           data-bs-toggle="dropdown"
                           aria-expanded="false">
                           Mã
                        </a>
                    </th>
                    <th>
                        @* <a class="nav-link dropdown-toggle"
                           href=""
                           role="button"
                           data-bs-toggle="dropdown"
                           aria-expanded="false">
                            Phòng ban
                        </a> *@
                        <select class="form-select" id="departmentFilter" name="departmentFilter">
                            <option value="" hidden>Chọn phòng ban</option>
                            @foreach (var department in ViewBag.uniquePhongBan)
                            {
                                <option value="@department">@department</option>
                            }
                        </select>
                    </th>
                    <th>
                        @* <a class="nav-link dropdown-toggle"
                           href=""
                           role="button"
                           data-bs-toggle="dropdown"
                           aria-expanded="false">
                            Chức vụ
                        </a> *@
                        <select class="form-select" id="titleFilter" name="titleFilter">
                            <option value="" hidden>Chọn chức vụ</option>
                            @foreach (var title in ViewBag.uniqueChucVu)
                            {
                                <option value="@title">@title</option>
                            }
                        </select>
                    </th>
                    <th>
                       @*  <a class="nav-link dropdown-toggle"
                           href=""
                           role="button"
                           data-bs-toggle="dropdown"
                           aria-expanded="false">
                            Vị trí
                        </a> *@
                        <select class="form-select" id="positionFilter" name="positionFilter">
                            <option value="" hidden
                                    class="nav-link dropdown-toggle"
                                    href=""
                                    role="button"
                                    data-bs-toggle="dropdown"
                                    aria-expanded="false">Chọn vị trí</option>
                            @foreach (var position in ViewBag.uniqueViTri)
                            {
                                <option value="@position">@position</option>
                            }
                        </select>
                    </th>
                    <th>
                        <a asp-controller="Info" asp-action="Index">
                            <span class="material-symbols-outlined" style="color:black">
                                layers_clear
                            </span>
                        </a>
                    </th>
                </tr>
            </thead>
            <tbody class="table-border-bottom-0">
                @foreach (var nv in Model)
                {
                    <tr style="font-size: 14px;">
                        <td data-id="@nv.Id" onclick="redirectToDetail('@nv.Id')">
                            <ul class="list-unstyled users-list m-0 avatar-group d-flex align-items-center">
                                <li data-bs-toggle="tooltip"
                                    data-popup="tooltip-custom"
                                    data-bs-placement="top"
                                    class="avatar avatar-xs pull-up"
                                    title="@nv.Ten">
                                    <img src="~/assets/images/default-avatar.png" alt="Avatar" class="rounded-circle"
                                         class="mini-profile-image" />
                                </li>
                            </ul>
                        </td>
                        <td data-id="@nv.Id" onclick="redirectToDetail('@nv.Id')"><i class="fab fa-angular fa-lg text-danger me-3"></i> <strongs>@nv.HoDem @nv.Ten</strongs></td>
                        <td data-id="@nv.Id" onclick="redirectToDetail('@nv.Id')">@nv.Id</td>
                        <td>@nv.TenPhongBan</td>
                        <td>@nv.TenChucVu</td>
                        <td>@nv.TenVitri</td>
                        <td>
                            <div class="dropdown">
                                <button type="button" class="btn p-0 dropdown-toggle hide-arrow" data-bs-toggle="dropdown"
                                        style="color:black">
                                    <span class="material-symbols-outlined">
                                        more_horiz
                                    </span>
                                </button>
                                <div class="dropdown-menu">
                                    <a class="dropdown-item" data-id="@nv.Id" onclick="redirectToDetail('@nv.Id')">
                                        <span class="material-symbols-outlined">
                                            edit
                                        </span>
                                        Thông tin
                                    </a>
                                    <a style="color:red; justify-content:center" class="dropdown-item" data-id="@nv.Id" onclick="redirectToDetail('@nv.Id')">
                                        <span class="material-symbols-outlined">
                                            delete
                                        </span>
                                        <strong>Xóa</strong>
                                    </a>
                                </div>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    @* <div class="demo-inline-spacing">
        <nav aria-label="Page navigation">
            <ul class="pagination pagination-sm">
                <li class="page-item  @(Model.PageIndex == 1 ? "disabled" : "")">
                    <a class="page-link" asp-route-pageNumber="1">
                        <span><<</span>
                    </a>
                </li>

                <li class="page-item @(Model.HasPreviousPage? "" : "disabled")">
                    <a class="page-link" asp-route-pageNumber="@(Model.PageIndex - 1)">
                        <span>Trước</span>
                    </a>
                </li>
                @for(int i = 1; i<= Model.TotalPages; i++)
                {
                    <li class="page-item @(Model.PageIndex == i ? "active" : "")">
                        <a class="page-link" asp-route-pageNumber="@i">@i</a>
                    </li>
                }
                <li class="page-item">
                    <a class="page-link" asp-route-pageNumber="@(Model.PageIndex + 1)">
                        <span>Sau</span>
                    </a>
                </li>

                <li class="page-item  @(Model.PageIndex == Model.TotalPages ? "disabled" : "")">
                    <a class="page-link" asp-route-pageNumber="@(Model.TotalPages)">
                        <span>>></span>
                    </a>
                </li>
            </ul>
        </nav>
    </div> *@
</div>